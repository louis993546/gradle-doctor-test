buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath "com.osacky.doctor:doctor-plugin:0.2.1"
    }
}

rootProject.plugins.apply(com.osacky.doctor.DoctorPlugin.class)

doctor {
    ensureJavaHomeMatches = false
    ensureJavaHomeIsSet = false
}